import chai from 'chai'
import divide from '../src/divide.js'

describe('divide()', function() {
    // DOESNT PASS - SHOULD PASS: always return default value
    // it ('Should return integer when even number divided by two', () => {
    //     chai.expect(divide(4, 2)).to.deep.equal(2);
    // });
    // it ('Should return 1.5 for 6/4', () => {
    //     chai.expect(divide(6, 4)).to.deep.equal(1.5);
    // });
    // it ('Return float <0 if dividend smaller than divisor', () => {
    //     chai.expect(divide(1, 10)).to.deep.equal(0.1);
    // });
});