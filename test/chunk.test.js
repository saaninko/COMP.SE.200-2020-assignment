import chai from 'chai'
import chunk from '../src/chunk.js'

describe('chunk()', function() {
    // DOESNT WORK - SHOULD WORK: doesnt include the first array
    // it ('Should split an array of 4 items into arrays of 2 items evenly.', () => {
    //     chai.expect(chunk(['a', 'b', 'c', 'd'], 2)).to.deep.equal([['a', 'b'], ['c', 'd']]);
    // });
});